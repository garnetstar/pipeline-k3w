name: Test nightly
on:
  schedule:
    - cron: '0/10 * * * *'
    - cron: '5/10 * * * *'

jobs:

  test:
    runs-on:
      - ubuntu-latest
    steps:
      - name: setenv
        if: github.event.schedule=='0/10 * * * *'
        run: |
          echo "::set-env name=cronenv::even"
      - name: setenv2
        if: github.event.schedule=='5/10 * * * *'
        run: |
          echo "::set-env name=cronenv::odd"
      - name: check env value
        run: |
          echo $cronenv  
