name: build-pipeline
on: workflow_call
jobs:
  build1:
    runs-on: ubuntu-latest
    steps:
      - run: cat /etc/os-release
  build2:
    runs-on: ubuntu-latest
    steps:
      - run: cat /etc/os-release
  build3:
    runs-on: ubuntu-latest
    steps:
      - run: cat /etc/os-release
